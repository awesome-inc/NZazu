<UserControl x:Class="NZazuFiddle.PreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:nZazu="clr-namespace:NZazu;assembly=NZazu"
             xmlns:nZazuFiddle="clr-namespace:NZazuFiddle"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance nZazuFiddle:PreviewViewModel}">
    <Grid>
        <nZazu:NZazuView x:Name="View" 
            FormDefinition="{Binding Definition}" 
            FormData="{Binding Data, Mode=TwoWay}" 
            LostFocus="ApplyChanges"
            FieldFactory="{Binding FieldFactory}"
            LayoutStrategy="{Binding LayoutStrategy}"/>
    </Grid>
</UserControl>
