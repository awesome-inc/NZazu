<UserControl x:Class="NZazuFiddle.AddTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NZazuFiddle"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="250">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0" />
            <Setter Property="Width" Value="55" />
            <Setter Property="Height" Value="24" />
        </Style>
    </UserControl.Resources>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="20"/>
            <RowDefinition Height="Auto" MinHeight="20"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Margin="8 8 8 4" x:Name="SampleId" Height="24" MinWidth="250" HorizontalAlignment="Left"/>
        <Button Grid.Row="1" Margin="8 4 8 8" FontWeight="Bold" FontSize="15" Content="+" x:Name="AddSample" HorizontalAlignment="Left"/>

    </Grid>
</UserControl>
