<UserControl x:Class="NZazuFiddle.FileMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NZazuFiddle"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Menu DockPanel.Dock="Top">
        <Menu.Resources>
            <fa:IconToImageConverter x:Key="IconToImage"/>
        </Menu.Resources>
        <MenuItem Header="_File">
            <MenuItem Header="_New" x:Name="NewFile" Icon="{Binding NewFileIcon, Converter={StaticResource IconToImage}}"/>
            <MenuItem Header="_Import" x:Name="ImportFile" Icon="{Binding ImportIcon, Converter={StaticResource IconToImage}}"/>
            <MenuItem Header="_Import folder" x:Name="ImportFiles" Icon="{Binding ImportIcon, Converter={StaticResource IconToImage}}"/>
            <MenuItem Header="_Export all" x:Name="ExportToFiles" Icon="{Binding ExportIcon, Converter={StaticResource IconToImage}}"/>
            <MenuItem Header="_Delete all" x:Name="DeleteAll" Icon="{Binding DeleteAllIcon, Converter={StaticResource IconToImage}}"/>
        </MenuItem>
    </Menu>

</UserControl>
