<UserControl x:Class="Sample.NZazuSampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:nZazu="clr-namespace:NZazu;assembly=NZazu"
             xmlns:sample="clr-namespace:Sample"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance sample:NZazuSampleViewModel}">
    <Grid>
        <nZazu:NZazuView x:Name="NZazuView" FormDefinition="{Binding FormDefinition}" FormData="{Binding FormData}"/>
    </Grid>
</UserControl>
